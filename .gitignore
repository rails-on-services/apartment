*.gem
.bundle
*.lock
gemfiles/*.lock
gemfiles/vendor
pkg/*
*.log
.idea
*.sw[pno]
spec/config/database.yml
cookbooks
tmp
spec/dummy/db/*.sqlite3
.DS_Store
# VS Code files
.vscode/*
# Claude Code local settings (user-specific)
.claude/settings.local.json
# Firecrawl working data
.firecrawl/
coverage/
# code-graph-mcp index (generated, per-machine)
.code-graph/
# Repomix packed output (generated, can be 10MB+)
repomix-output.*
# Serena auto-generates project.yml and rewrites its schema/comment block on every
# version bump, so a tracked copy churns in unrelated PRs. Let each checkout self-configure.
# Canonical language set is documented in docs/ai-tools-setup.md.
.serena/
